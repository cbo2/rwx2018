const foo = function(n) {
  if(n < 5) {
    return 
      n * 2; //since this may standalone, JavaScript inserted an invisible ; at the end of return
  } else {
    return n;
  }
}

console.log(foo(6));
console.log(foo(3));